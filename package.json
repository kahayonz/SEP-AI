{
  "name": "sep-ai",
  "version": "1.0.0",
  "description": "Software Engineering Evaluation with AI",
  "scripts": {
    "dev": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "backend": "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step needed for this project'",
    "start": "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}
